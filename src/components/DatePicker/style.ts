import {StyleSheet} from 'react-native'

export const dateItemStyles = StyleSheet.create({
  wrapper: {
    flex: 1,
    paddingVertical: 6
  },
  text: {
    borderRadius: 10,
    textAlign: 'center',
    color: '#555',
    fontSize: 14
  }
})
